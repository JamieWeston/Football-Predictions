name: Advanced Premier League Predictions

on:
  schedule:
    - cron: '0 8 * * *'  # Daily at 8 AM UTC
  workflow_dispatch:

jobs:
  predict:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install requests
    
    - name: Check API Keys
      env:
        RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
        ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
      run: |
        echo "Checking API keys..."
        [ -n "$RAPIDAPI_KEY" ] && echo "✅ RapidAPI key found" || echo "❌ RapidAPI key missing"
        [ -n "$ODDS_API_KEY" ] && echo "✅ Odds API key found" || echo "❌ Odds API key missing"
        [ -n "$NEWS_API_KEY" ] && echo "✅ News API key found" || echo "❌ News API key missing"
    
    - name: Fetch All Data Sources
      env:
        RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
        RAPIDAPI_HOST: ${{ secrets.RAPIDAPI_HOST }}
        ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
      run: |
        echo "Starting complete data collection..."
        python src/fetch_all_data.py
    
    - name: Generate Advanced Predictions
      run: |
        echo "Generating predictions with all data..."
        python src/predict_advanced.py
    
    - name: Show Results
      run: |
        echo "=== Enhanced Data ==="
        if [ -f data/enhanced_fixtures.json ]; then
          echo "Enhanced fixtures size: $(wc -c < data/enhanced_fixtures.json) bytes"
        fi
        echo ""
        echo "=== Predictions Preview ==="
        if [ -f predictions.json ]; then
          head -100 predictions.json
        fi
    
    - name: Commit and Push
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add -A
        git diff --quiet && git diff --staged --quiet || (
          git commit -m "🎯 Advanced PL predictions - $(date +'%Y-%m-%d')"
          git push
        )
